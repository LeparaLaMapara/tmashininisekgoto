# Optional search-engine ownership verification. Set these in Vercel once the
# codes are issued; the layout emits the matching meta tags automatically. Empty
# or unset means no tag is emitted, which is the correct default.
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=
NEXT_PUBLIC_BING_SITE_VERIFICATION=
